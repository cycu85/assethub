{% extends 'base.html.twig' %}

{% block title %}Moje przypisania - {{ app_name() }}{% endblock %}
{% block page_title %}Moje przypisania{% endblock %}

{% block content %}
<div class="row mb-4">
    <div class="col-md-8">
        <h4 class="mb-0">
            <i class="ri-user-line me-2 text-primary"></i>
            Mój sprzęt asekuracyjny
        </h4>
        <p class="text-muted mb-0">Sprzęt i zestawy przypisane do mnie</p>
    </div>
    <div class="col-md-4 text-end">
        <a href="{{ path('asekuracja_index') }}" class="btn btn-outline-secondary">
            <i class="ri-arrow-left-line me-1"></i>
            Powrót do listy
        </a>
    </div>
</div>

<!-- Statystyki moich przypisań -->
<div class="row mb-4">
    <div class="col-md-3">
        <div class="card border-0 shadow-sm">
            <div class="card-body">
                <div class="d-flex align-items-center">
                    <div class="avatar-sm bg-primary rounded">
                        <i class="ri-shield-line text-white fs-16"></i>
                    </div>
                    <div class="ms-3">
                        <h5 class="mb-0">{{ equipment|length }}</h5>
                        <p class="text-muted mb-0">Pojedynczy sprzęt</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card border-0 shadow-sm">
            <div class="card-body">
                <div class="d-flex align-items-center">
                    <div class="avatar-sm bg-info rounded">
                        <i class="ri-stack-line text-white fs-16"></i>
                    </div>
                    <div class="ms-3">
                        <h5 class="mb-0">{{ equipment_sets|length }}</h5>
                        <p class="text-muted mb-0">Zestawy sprzętu</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card border-0 shadow-sm">
            <div class="card-body">
                <div class="d-flex align-items-center">
                    <div class="avatar-sm bg-warning rounded">
                        <i class="ri-time-line text-white fs-16"></i>
                    </div>
                    <div class="ms-3">
                        {% set needing_review = 0 %}
                        {% for item in equipment %}
                            {% if item.needsReview %}
                                {% set needing_review = needing_review + 1 %}
                            {% endif %}
                        {% endfor %}
                        {% for set in equipment_sets %}
                            {% if set.needsReview %}
                                {% set needing_review = needing_review + 1 %}
                            {% endif %}
                        {% endfor %}
                        <h5 class="mb-0">{{ needing_review }}</h5>
                        <p class="text-muted mb-0">Wymagają przeglądu</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card border-0 shadow-sm">
            <div class="card-body">
                <div class="d-flex align-items-center">
                    <div class="avatar-sm bg-success rounded">
                        <i class="ri-check-line text-white fs-16"></i>
                    </div>
                    <div class="ms-3">
                        <h5 class="mb-0">{{ (equipment|length + equipment_sets|length) - needing_review }}</h5>
                        <p class="text-muted mb-0">Aktualne przeglądy</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Pojedynczy sprzęt -->
{% if equipment|length > 0 %}
<div class="row mb-4">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h5 class="card-title mb-0">
                    <i class="ri-shield-line me-2"></i>
                    Przypisany sprzęt ({{ equipment|length }})
                </h5>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-hover mb-0">
                        <thead class="table-light">
                            <tr>
                                <th>Sprzęt</th>
                                <th>Typ</th>
                                <th>Data przypisania</th>
                                <th>Następny przegląd</th>
                                <th>Status</th>
                                <th class="text-end">Akcje</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for item in equipment %}
                            <tr>
                                <td>
                                    <div class="d-flex align-items-center">
                                        <div class="avatar-sm bg-light rounded me-3">
                                            <i class="ri-shield-line text-primary fs-16"></i>
                                        </div>
                                        <div>
                                            <h6 class="mb-0">{{ item.name }}</h6>
                                            <span class="text-muted small">{{ item.inventoryNumber }}</span>
                                            {% if item.manufacturer and item.model %}
                                            <br><span class="text-muted small">{{ item.manufacturer }} {{ item.model }}</span>
                                            {% endif %}
                                        </div>
                                    </div>
                                </td>
                                <td>
                                    <span class="badge bg-secondary">{{ item.equipmentType }}</span>
                                </td>
                                <td>
                                    {{ item.assignedDate|date('d.m.Y') }}
                                </td>
                                <td>
                                    {% if item.nextReviewDate %}
                                        {% set review_date = item.nextReviewDate|date('Y-m-d') %}
                                        {% set today_date = 'now'|date('Y-m-d') %}
                                        {% if review_date < today_date %}
                                            {% set days_overdue = date().diff(date(item.nextReviewDate)).days %}
                                            <span class="text-danger">
                                                <i class="ri-error-warning-line me-1"></i>
                                                {{ item.nextReviewDate|date('d.m.Y') }}
                                                <br><small>Przeterminowany o {{ days_overdue }} dni!</small>
                                            </span>
                                        {% else %}
                                            {% set days_to_review = date(item.nextReviewDate).diff(date()).days %}
                                            {% if days_to_review <= 30 %}
                                                <span class="text-warning">
                                                    <i class="ri-time-line me-1"></i>
                                                    {{ item.nextReviewDate|date('d.m.Y') }}
                                                    <br><small>Za {{ days_to_review }} dni</small>
                                                </span>
                                            {% else %}
                                                <span class="text-muted">
                                                    {{ item.nextReviewDate|date('d.m.Y') }}
                                                    <br><small>Za {{ days_to_review }} dni</small>
                                                </span>
                                            {% endif %}
                                        {% endif %}
                                    {% else %}
                                        <span class="text-muted">Nie określono</span>
                                    {% endif %}
                                </td>
                                <td>
                                    {% set status_class = item.status == 'available' ? 'success' : 
                                                         (item.status == 'assigned' ? 'warning' : 
                                                         (item.status == 'damaged' ? 'danger' : 'secondary')) %}
                                    <span class="badge bg-{{ status_class }}">{{ item.statusDisplayName }}</span>
                                </td>
                                <td class="text-end">
                                    <a href="{{ path('asekuracja_equipment_show', {id: item.id}) }}" 
                                       class="btn btn-outline-primary btn-sm">
                                        <i class="ri-eye-line me-1"></i>Szczegóły
                                    </a>
                                </td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
{% endif %}

<!-- Zestawy sprzętu -->
{% if equipment_sets|length > 0 %}
<div class="row mb-4">
    <div class="col-12">
        <div class="card">
            <div class="card-header">
                <h5 class="card-title mb-0">
                    <i class="ri-stack-line me-2"></i>
                    Przypisane zestawy ({{ equipment_sets|length }})
                </h5>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table table-hover mb-0">
                        <thead class="table-light">
                            <tr>
                                <th>Zestaw</th>
                                <th>Typ</th>
                                <th>Elementy</th>
                                <th>Data przypisania</th>
                                <th>Następny przegląd</th>
                                <th>Status</th>
                                <th class="text-end">Akcje</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for set in equipment_sets %}
                            <tr>
                                <td>
                                    <div class="d-flex align-items-center">
                                        <div class="avatar-sm bg-light rounded me-3">
                                            <i class="ri-stack-line text-primary fs-16"></i>
                                        </div>
                                        <div>
                                            <h6 class="mb-0">{{ set.name }}</h6>
                                            {% if set.description %}
                                            <span class="text-muted small">{{ set.description|slice(0, 60) }}{% if set.description|length > 60 %}...{% endif %}</span>
                                            {% endif %}
                                        </div>
                                    </div>
                                </td>
                                <td>
                                    <span class="badge bg-info">{{ set.setType }}</span>
                                </td>
                                <td>
                                    <div class="d-flex align-items-center">
                                        <span class="fw-medium">{{ set.equipmentItems|length }}</span>
                                        <span class="text-muted ms-1">elementów</span>
                                        {% if set.isComplete %}
                                        <i class="ri-check-line text-success ms-2" title="Zestaw kompletny"></i>
                                        {% else %}
                                        <i class="ri-error-warning-line text-warning ms-2" title="Zestaw niekompletny"></i>
                                        {% endif %}
                                    </div>
                                    {% if set.equipmentItems|length > 0 %}
                                    <div class="mt-1">
                                        {% for item in set.equipmentItems|slice(0, 3) %}
                                        <small class="badge bg-light text-dark me-1">{{ item.equipment.equipmentType }}</small>
                                        {% endfor %}
                                        {% if set.equipmentItems|length > 3 %}
                                        <small class="text-muted">+{{ set.equipmentItems|length - 3 }} więcej</small>
                                        {% endif %}
                                    </div>
                                    {% endif %}
                                </td>
                                <td>
                                    {{ set.assignedDate|date('d.m.Y') }}
                                </td>
                                <td>
                                    {% if set.nextReviewDate %}
                                        {% set review_date = set.nextReviewDate|date('Y-m-d') %}
                                        {% set today_date = 'now'|date('Y-m-d') %}
                                        {% if review_date < today_date %}
                                            {% set days_overdue = date().diff(date(set.nextReviewDate)).days %}
                                            <span class="text-danger">
                                                <i class="ri-error-warning-line me-1"></i>
                                                {{ set.nextReviewDate|date('d.m.Y') }}
                                                <br><small>Przeterminowany o {{ days_overdue }} dni!</small>
                                            </span>
                                        {% else %}
                                            {% set days_to_review = date(set.nextReviewDate).diff(date()).days %}
                                            {% if days_to_review <= 30 %}
                                                <span class="text-warning">
                                                    <i class="ri-time-line me-1"></i>
                                                    {{ set.nextReviewDate|date('d.m.Y') }}
                                                    <br><small>Za {{ days_to_review }} dni</small>
                                                </span>
                                            {% else %}
                                                <span class="text-muted">
                                                    {{ set.nextReviewDate|date('d.m.Y') }}
                                                    <br><small>Za {{ days_to_review }} dni</small>
                                                </span>
                                            {% endif %}
                                        {% endif %}
                                    {% else %}
                                        <span class="text-muted">Nie określono</span>
                                    {% endif %}
                                </td>
                                <td>
                                    {% set status_class = set.status == 'available' ? 'success' : 
                                                         (set.status == 'assigned' ? 'warning' : 
                                                         (set.status == 'in_review' ? 'info' : 'secondary')) %}
                                    <span class="badge bg-{{ status_class }}">{{ set.statusDisplayName }}</span>
                                </td>
                                <td class="text-end">
                                    <a href="{{ path('asekuracja_equipment_set_show', {id: set.id}) }}" 
                                       class="btn btn-outline-primary btn-sm">
                                        <i class="ri-eye-line me-1"></i>Szczegóły
                                    </a>
                                </td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
{% endif %}

<!-- Brak przypisań -->
{% if equipment|length == 0 and equipment_sets|length == 0 %}
<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-body">
                <div class="text-center py-5">
                    <i class="ri-user-unfollow-line text-muted fs-48"></i>
                    <h5 class="mt-3">Brak przypisanego sprzętu</h5>
                    <p class="text-muted">
                        Nie masz obecnie przypisanego żadnego sprzętu ani zestawów asekuracyjnych.
                    </p>
                    <a href="{{ path('asekuracja_index') }}" class="btn btn-primary">
                        <i class="ri-arrow-left-line me-1"></i>
                        Powrót do głównej listy
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endif %}

<!-- Ważne informacje -->
{% if equipment|length > 0 or equipment_sets|length > 0 %}
<div class="row mt-4">
    <div class="col-12">
        <div class="alert alert-info">
            <h6 class="alert-heading">
                <i class="ri-information-line me-2"></i>Ważne informacje
            </h6>
            <ul class="mb-0">
                <li>Regularnie sprawdzaj terminy przeglądów swojego sprzętu</li>
                <li>Zgłaszaj wszelkie uszkodzenia lub nieprawidłowości natychmiast</li>
                <li>Nie używaj sprzętu po terminie przeglądu lub w przypadku uszkodzenia</li>
                <li>W razie pytań skontaktuj się z administratorem modułu Asekuracja</li>
            </ul>
        </div>
    </div>
</div>
{% endif %}
{% endblock %}